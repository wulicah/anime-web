name = "fanlu-bgm"
main = "src/index.ts"
compatibility_date = "2024-09-01"

# 部署前无需修改任何东西，wrangler deploy 会自动搞定
# 部署后拿到 URL（形如 https://fanlu-bgm.你的名字.workers.dev）
# 把这个 URL 填到项目根目录 .env 的 VITE_BANGUMI_BASE_URL 后面

[observability]
enabled = true
