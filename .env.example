# Bangumi API 代理配置
#
# 使用方式：
#   1. 复制此文件为 .env
#   2. 根据需要取消下面配置的注释并填入实际值
#
# 重要：前端代码默认走相对路径 /api/bgm 和 /img
#   - dev: Vite proxy 中转（见 vite.config.ts）
#   - prod: Cloudflare Pages Functions 中转（functions/ 目录）
# 不需要额外配置即可运行

# 开发环境代理（dev 时 Vite proxy 转发到这些地址）
# 不配置时 dev 无法加载番剧数据（需要能直连 api.bgm.tv）
# VITE_PROXY_TARGET=https://your-proxy.example.com
# VITE_IMG_PROXY_TARGET=https://your-img-proxy.example.com

# 前端 BASE_URL 留空 = 走相对路径 /api/bgm 和 /img
# 只在以下情况设置此变量：
#   ① 部署到非 Cloudflare Pages 平台，且没有配置 rewrites
#   ② 使用自定义代理域名
# VITE_BANGUMI_BASE_URL=
